# ============================================================
# REG-X Frontend — Variables de entorno (Vite)
# Copia este archivo a .env (o .env.local) y complétalo.
# Solo llaves PÚBLICAS: nunca pongas secretos aquí.
# ============================================================

# ── Supabase ─────────────────────────────────────────────────
VITE_SUPABASE_URL=https://ofsgenbpqfrcyvtiannb.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...tu_anon_key

# ── Wompi (opcional) ─────────────────────────────────────────
# Con la llave pública presente, aparece el botón "Pagar en línea con Wompi".
# Si la dejas vacía, la app usa el cobro manual automáticamente.
# Los secretos (integridad/eventos/privada) van en las Edge Functions, no aquí.
VITE_WOMPI_PUBLIC_KEY=
VITE_WOMPI_ENV=sandbox
